---
import { css } from '~panda/css';

type Props = astroHTML.JSX.DefinedIntrinsicElements['p'];

const props = Astro.props;
---

<p
  {...props}
  class={css({
    fontSize: 'md',
    fontFamily: 'dosis',
    w: '100%',
  })}
>
  <slot />
</p>
