---
import { table } from 'styled-system/recipes';
import { css } from 'styled-system/css';
---

<td
  class:list={[
    table().cell,
    css({
      py: '2',
      minW: 'fit-content',
      textAlign: 'center',
      textWrap: 'nowrap',
    }),
  ]}
>
  <slot />
</td>
