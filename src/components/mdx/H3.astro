---
import { css } from '~panda/css';

type Props = astroHTML.JSX.DefinedIntrinsicElements['h3'];

const props = Astro.props;
---

<h3
  {...props}
  class={css({
    fontSize: '2xl',
    fontFamily: 'dosis',
    w: '100%',
  })}
>
  <slot />
</h3>
