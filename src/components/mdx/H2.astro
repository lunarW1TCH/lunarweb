---
import { css } from '~panda/css';

type Props = astroHTML.JSX.DefinedIntrinsicElements['h2'];

const props = Astro.props;
---

<h2
  {...props}
  class={css({
    fontSize: '3xl',
    fontFamily: 'dosis',
    w: '100%',
  })}
>
  <slot />
</h2>
