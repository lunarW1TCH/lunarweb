---
import { css } from '~panda/css';

type Props = astroHTML.JSX.DefinedIntrinsicElements['h4'];

const props = Astro.props;
---

<h4
  {...props}
  class={css({
    fontSize: 'xl',
    fontFamily: 'dosis',
    w: '100%',
  })}
>
  <slot />
</h4>
