---
import { table } from 'styled-system/recipes';
import { css } from 'styled-system/css';
---

<tr
  class:list={[
    table().row,
    css({
      borderY: '1px solid',
      borderColor: 'border.muted',
      _hover: {
        bgColor: 'bg.muted',
      },
    }),
  ]}
>
  <slot />
</tr>
