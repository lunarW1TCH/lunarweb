---
import { table } from '~panda/recipes';
import { css } from '~panda/css';
---

<tr
  class:list={[
    table().row,
    css({
      borderY: '1px solid',
      borderColor: 'border.muted',
      _hover: {
        bgColor: 'bg.muted',
      },
    }),
  ]}
>
  <slot />
</tr>
