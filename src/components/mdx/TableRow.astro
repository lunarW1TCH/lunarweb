---
import * as TableUI from '~/components/ui/table';
import { css } from '~panda/css';
---

<TableUI.Row
  className={css({
    borderY: '1px solid',
    borderColor: 'border.muted',
    _hover: {
      bgColor: 'bg.muted',
    },
  })}
>
  <slot />
</TableUI.Row>
